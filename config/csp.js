// These are expected to contain the raw nonce token (not a full Content-Security-Policy header).
export const CSP_NONCE_HEADER_NAMES = ['x-csp-nonce', 'csp-nonce', 'nonce'];
export const CSP_NONCE_META_NAME = 'csp-nonce';
